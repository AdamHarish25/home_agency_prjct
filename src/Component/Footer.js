

export const Footer = ({bgColor = "bg-transparent"}) => {
    const className = {
        container: `${bgColor} w-full space-y-10 p-10`
    }


    return (
        <div className={className.container}>
            
        </div>
    )
}